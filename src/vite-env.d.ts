/// <reference types="vite/client" />

/**
 * Debug mode flag set by Vite at build time.
 * true when built with --mode development
 * false for production builds
 */
declare const __DEBUG_MODE__: boolean;
